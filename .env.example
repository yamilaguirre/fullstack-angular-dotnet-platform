# Copy this file to `.env` at the repo root to override Docker SQL SA password (optional).
# Default used by docker-compose matches backend/src/Evalua.Api/appsettings.Development.json.
# Strong password: upper, lower, digit, symbol, length ≥ 8.
MSSQL_SA_PASSWORD=Evalua_Dev_2026!
